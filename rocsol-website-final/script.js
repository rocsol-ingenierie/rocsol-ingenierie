const menuBtn=document.querySelector('.menu-btn');const nav=document.querySelector('.nav');if(menuBtn){menuBtn.addEventListener('click',()=>nav.classList.toggle('open'));}
const form=document.querySelector('#contact-form');if(form){form.addEventListener('submit',e=>{e.preventDefault();const f=new FormData(form);const subject=encodeURIComponent(f.get('objet')||'Demande via le site ROCSOL INGENIERIE');const body=encodeURIComponent(`Nom: ${f.get('nom')}\nSociété: ${f.get('societe')}\nEmail: ${f.get('email')}\nTéléphone: ${f.get('telephone')}\n\n${f.get('message')}`);window.location.href=`mailto:rocsol.ing@gmail.com?subject=${subject}&body=${body}`;});}
